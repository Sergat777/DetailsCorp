<Page x:Class="DetailsCorp.Pages.StorySupPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:DetailsCorp.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="StorySupPage">

    <Grid>
        <DataGrid x:Name="ViewStorySup" AutoGenerateColumns="False" ColumnWidth="*" >
            <DataGrid.Columns>
                <DataGridTextColumn Header="Наименование детали" Binding="{Binding Detail.NameDetail}"/>
                <DataGridTextColumn Header="Наименование поставщика" Binding="{Binding Supplier.NameSupplier}"/>
                <DataGridTextColumn Header="Дата поставки" Binding="{Binding DateSale}"/>
                <DataGridTextColumn Header="Количество деталей" Binding="{Binding NumberDetails}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>
